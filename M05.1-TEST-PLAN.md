# 3ELocal M05.1 Test Plan

## 1. التحديث

1. ابنِ Build 8 وثبته فوق M04 بنفس Bundle ID.
2. تأكد من بقاء التطبيقات والعداد والمتجر والتوقيعات السابقة.

## 2. استيراد المفتاح الرئيسي

1. افتح الإعدادات → إدارة مفاتيح التوقيع.
2. اختر ملف `3E-Publisher-PrivateKey.3ekey` من حزمة التوقيع المنفصلة.
3. اختر مدة سنة وفعّل Face ID.
4. تأكد من ظهور المفتاح `3e-main-2026-01` بحالة فعال.

## 3. التوقيع من الآيفون

1. افتح تفاصيل Hello3E المحلي.
2. اختر «توقيع وتصدير من الآيفون».
3. اختر المفتاح و«بدون انتهاء» للحزمة.
4. وافق باستخدام Face ID أو رمز الجهاز.
5. تأكد من إنشاء الملف داخل `Apps/LocalWeb/Packages/Signed`.
6. شارك الملف أو أعد تثبيته داخل 3ELocal. يجب أن يظهر كحزمة موثوقة.

## 4. الصلاحيات

- أنشئ مفتاحًا لمدة يوم واحد وتأكد من عرض تاريخ الانتهاء.
- أنشئ حزمة بصلاحية يوم واحد وتأكد من كتابة التاريخ في تفاصيل نتيجة التوقيع.
- لا يلزم انتظار يوم للاختبار؛ افحص `checksums.json` داخل نسخة تجريبية إن لزم.

## 5. Keys/Inbox

1. ضع نسخة من `.3ekey` في `Apps/LocalWeb/Keys/Inbox`.
2. استخدم الاستيراد من Inbox.
3. تأكد من حذف الملف من Inbox بعد نجاح الاستيراد.

## 6. حذف المفتاح الخاص

1. اسحب المفتاح واختر «حذف الخاص».
2. تأكد من تغير الحالة إلى «تحقق فقط».
3. تأكد من استمرار ظهور التطبيقات القديمة الموقعة بهذا المفتاح.
4. تأكد من عدم إمكانية استخدام المفتاح لتوقيع حزمة جديدة.
