# خطة اختبار 3ELocal M04

الإصدار: `0.2.0-M04` — البناء `7`

## 1. اختبار الترقية

- ثبّت M04 فوق M03 بنفس Bundle ID.
- تأكد أن التطبيقات والمشاريع والعداد القديم باقية.
- التطبيقات المحلية القديمة يجب أن تظهر بعلامة **غير موقّع** وتستمر في العمل.

## 2. إصلاح لوحة مفاتيح المتجر

افتح المتجر والمس حقل رابط `catalog.json` ثم اختبر كل حالة:

1. اضغط **تم** أعلى لوحة المفاتيح.
2. اضغط Enter/انتقال.
3. اضغط زر **فحص التطبيقات والتحديثات**.
4. اسحب قائمة المتجر لأسفل.
5. اضغط في مكان فارغ خارج حقل الرابط.
6. انتقل إلى تبويب آخر ثم عد إلى المتجر.

في جميع الحالات يجب أن تختفي لوحة المفاتيح، ولا يلزم إغلاق التطبيق.

رابط الفهرس المستخدم في التجارب السابقة:

```text
https://raw.githubusercontent.com/esshoo/3ELocal-iOS/refs/heads/main/TestCatalog/catalog.json
```

## 3. تثبيت الحزمة الموقعة

- ارفع مجلد `TestCatalog` الجديد إلى المستودع، أو استورد `Hello3E-v1.3.0-Signed.3eweb` يدويًا.
- نزّل وثبّت الإصدار `1.3.0`.
- يجب أن ينجح التثبيت دون تفعيل وضع المطور.
- افتح التفاصيل وتأكد من ظهور:
  - الحالة: حزمة موقعة وموثوقة.
  - الناشر: `3E / Essam`.
  - مفتاح التوقيع: `3e-main-2026-01`.
- تأكد أن العداد بقي محفوظًا بعد التحديث.

## 4. رفض الحزمة المعدلة

استورد `Hello3E-v1.3.0-Tampered.3eweb`.

النتيجة المتوقعة: رفض التثبيت برسالة تفيد بأن `www/index.html` تم تعديله بعد توقيع الحزمة. يجب أن يستمر الرفض حتى عند تفعيل وضع المطور.

## 5. وضع المطور والحزم القديمة

- من الإعدادات فعّل **السماح بالحزم غير الموقعة (وضع المطور)**.
- استورد حزمة M01 أو M02 غير موقعة. يجب أن تُقبل وتظهر بعلامة **غير موقّع**.
- أوقف وضع المطور. يجب أن تستمر التطبيقات القديمة المثبتة في العمل، لكن تثبيت حزمة جديدة غير موقعة يُرفض.

## 6. منع خفض الثقة

بعد تثبيت `Hello3E 1.3.0` الموقّع، حاول تثبيت إصدار غير موقّع له نفس المعرّف أثناء تفعيل وضع المطور. يجب رفض استبدال تطبيق موثوق بحزمة غير موقعة.
